# Copy to ~/.config/secrets/cursor.env and chmod 600.
# Optional: only needed if `cursor-agent login` (keychain) does not work for you
# -- e.g. you run agents in a sandbox that cannot read the macOS keychain.
# Get a key from your Cursor dashboard.
CURSOR_API_KEY=
